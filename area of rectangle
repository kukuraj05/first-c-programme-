#include <stdio.h>
int main(){
    //int length = 3;
    //int breadth = 6; 
    int length, breadth;
    printf("Enter length\n");
    scanf("%d",&length);

    printf("Enter breath\n");
    scanf("%d",&breadth);

    printf("the area of this rectangle is %d", length * breadth);
    if ("%d = 0" )
    {
     printf("the rectangle is a line ");
    }
    
return 0;
}
